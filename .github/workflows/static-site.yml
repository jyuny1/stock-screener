name: Static Site

on:
  workflow_dispatch:

concurrency:
  group: static-site-us
  cancel-in-progress: true

permissions:
  contents: write
  deployments: write

jobs:
  publish-us:
    if: ${{ github.ref == format('refs/heads/{0}', github.event.repository.default_branch) }}
    runs-on: ubuntu-latest
    env:
      STATIC_DATA_DIR: frontend/public/static-data

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: frontend/package-lock.json

      - name: Install frontend dependencies
        run: cd frontend && npm ci

      - name: Download foundation update artifact
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          mkdir -p /tmp/foundation-update
          FOUNDATION_UPDATE_RELEASE="foundation-update-data"
          FOUNDATION_UPDATE_MANIFEST="foundation-update-latest-us.json"
          if ! gh release download "$FOUNDATION_UPDATE_RELEASE" \
            --repo "${{ github.repository }}" \
            --pattern "$FOUNDATION_UPDATE_MANIFEST" \
            --dir /tmp/foundation-update \
            --clobber; then
            echo "No foundation-update-data release yet; falling back to legacy weekly-reference-data." >&2
            FOUNDATION_UPDATE_RELEASE="weekly-reference-data"
            FOUNDATION_UPDATE_MANIFEST="weekly-reference-latest-us.json"
            gh release download "$FOUNDATION_UPDATE_RELEASE" \
              --repo "${{ github.repository }}" \
              --pattern "$FOUNDATION_UPDATE_MANIFEST" \
              --dir /tmp/foundation-update \
              --clobber
          fi
          FOUNDATION_UPDATE_MANIFEST_PATH="/tmp/foundation-update/${FOUNDATION_UPDATE_MANIFEST}"
          export FOUNDATION_UPDATE_MANIFEST_PATH
          echo "FOUNDATION_UPDATE_RELEASE=${FOUNDATION_UPDATE_RELEASE}" >> "$GITHUB_ENV"
          echo "FOUNDATION_UPDATE_MANIFEST=${FOUNDATION_UPDATE_MANIFEST_PATH}" >> "$GITHUB_ENV"

          BUNDLE_ASSET_NAME="$(python - <<'PY'
          import json, os
          from pathlib import Path
          manifest = json.loads(Path(os.environ['FOUNDATION_UPDATE_MANIFEST_PATH']).read_text(encoding='utf-8'))
          coverage = manifest.get('coverage') or {}
          if manifest.get('market') != 'US':
              raise SystemExit(f"foundation update manifest market must be US, got {manifest.get('market')!r}")
          if coverage.get('universe_mode') != 'US_OPTIONABLE':
              raise SystemExit(f"foundation update manifest must use US_OPTIONABLE, got {coverage.get('universe_mode')!r}")
          if int(coverage.get('active_symbols') or 0) < 3000:
              raise SystemExit(f"foundation update manifest active_symbols too low: {coverage.get('active_symbols')}")
          if int(coverage.get('missing_active_symbols') or 0) != 0:
              raise SystemExit(f"foundation update manifest has missing active symbols: {coverage.get('missing_active_symbols')}")
          print(manifest['bundle_asset_name'])
          PY
          )"
          echo "FOUNDATION_UPDATE_BUNDLE=/tmp/foundation-update/${BUNDLE_ASSET_NAME}" >> "$GITHUB_ENV"
          echo "FOUNDATION_UPDATE_SHA256=$(python - <<'PY'
          import json, os
          from pathlib import Path
          print(json.loads(Path(os.environ['FOUNDATION_UPDATE_MANIFEST_PATH']).read_text(encoding='utf-8'))['sha256'])
          PY
          )" >> "$GITHUB_ENV"

          gh release download "$FOUNDATION_UPDATE_RELEASE" \
            --repo "${{ github.repository }}" \
            --pattern "${BUNDLE_ASSET_NAME}" \
            --dir /tmp/foundation-update \
            --clobber

      - name: Verify foundation update checksum
        run: |
          set -euo pipefail
          digest="$(python - <<'PY'
          import hashlib, os
          from pathlib import Path
          print(hashlib.sha256(Path(os.environ['FOUNDATION_UPDATE_BUNDLE']).read_bytes()).hexdigest())
          PY
          )"
          if [ "$digest" != "$FOUNDATION_UPDATE_SHA256" ]; then
            echo "Foundation update checksum mismatch: $digest != $FOUNDATION_UPDATE_SHA256" >&2
            exit 1
          fi

      - name: Download optional daily price artifact
        continue-on-error: true
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          mkdir -p /tmp/daily-price
          gh release download daily-price-data \
            --repo "${{ github.repository }}" \
            --pattern daily-price-latest-us.json \
            --dir /tmp/daily-price \
            --clobber

          BUNDLE_ASSET_NAME="$(python - <<'PY'
          import json
          from pathlib import Path
          manifest = json.loads(Path('/tmp/daily-price/daily-price-latest-us.json').read_text(encoding='utf-8'))
          if manifest.get('market') != 'US':
              raise SystemExit(f"daily manifest market must be US, got {manifest.get('market')!r}")
          print(manifest['bundle_asset_name'])
          PY
          )"
          echo "DAILY_PRICE_BUNDLE=/tmp/daily-price/${BUNDLE_ASSET_NAME}" >> "$GITHUB_ENV"
          echo "DAILY_PRICE_SHA256=$(python - <<'PY'
          import json
          from pathlib import Path
          print(json.loads(Path('/tmp/daily-price/daily-price-latest-us.json').read_text(encoding='utf-8'))['sha256'])
          PY
          )" >> "$GITHUB_ENV"

          gh release download daily-price-data \
            --repo "${{ github.repository }}" \
            --pattern "${BUNDLE_ASSET_NAME}" \
            --dir /tmp/daily-price \
            --clobber

      - name: Verify optional daily price checksum
        if: ${{ env.DAILY_PRICE_BUNDLE != '' }}
        run: |
          set -euo pipefail
          digest="$(python - <<'PY'
          import hashlib, os
          from pathlib import Path
          print(hashlib.sha256(Path(os.environ['DAILY_PRICE_BUNDLE']).read_bytes()).hexdigest())
          PY
          )"
          if [ "$digest" != "$DAILY_PRICE_SHA256" ]; then
            echo "Daily price checksum mismatch: $digest != $DAILY_PRICE_SHA256" >&2
            exit 1
          fi

      - name: Build static data from artifacts
        run: |
          set -euo pipefail
          rm -rf "$STATIC_DATA_DIR"
          args=(
            --foundation-update "$FOUNDATION_UPDATE_BUNDLE"
            --output-dir "../$STATIC_DATA_DIR"
          )
          if [ -n "${DAILY_PRICE_BUNDLE:-}" ]; then
            args+=(--daily-price "$DAILY_PRICE_BUNDLE")
          fi
          cd backend
          python -m app.scripts.build_static_site_from_artifacts "${args[@]}"

      - name: Validate static data contract
        run: |
          set -euo pipefail
          python - <<'PY'
          import json
          from pathlib import Path
          root = Path('frontend/public/static-data')
          manifest = json.loads((root / 'manifest.json').read_text(encoding='utf-8'))
          assert manifest['schema_version'] == 'static-site-v2'
          assert manifest['default_market'] == 'US'
          scan_path = root / manifest['markets']['US']['pages']['scan']['path']
          scan = json.loads(scan_path.read_text(encoding='utf-8'))
          assert scan['schema_version'] == 'static-scan-v1'
          assert scan['rows_total'] >= 3000
          assert scan['initial_rows']
          required = {'symbol', 'company_name', 'market', 'composite_score', 'volume', 'market_cap', 'gics_sector'}
          missing = required - set(scan['initial_rows'][0])
          if missing:
              raise SystemExit(f'missing required scan row fields: {sorted(missing)}')
          priced = 0
          for chunk in scan['chunks']:
              path = root / chunk['path']
              if not path.exists():
                  raise SystemExit(f'missing scan chunk: {path}')
              payload = json.loads(path.read_text(encoding='utf-8'))
              priced += sum(1 for row in payload.get('rows', []) if row.get('current_price') is not None)
          price_coverage = priced / scan['rows_total'] if scan['rows_total'] else 0
          if price_coverage < 0.80:
              raise SystemExit(f'static scan price coverage too low: {price_coverage:.2%}')
          print({'rows_total': scan['rows_total'], 'chunks': len(scan['chunks']), 'price_coverage': round(price_coverage, 6)})
          PY

      - name: Build static frontend
        env:
          VITE_STATIC_SITE: "true"
          VITE_BASE_PATH: /
          VITE_STATIC_DATA_BASE_URL: ${{ vars.STATIC_DATA_BASE_URL }}
        run: cd frontend && npm run build

      - name: Validate Cloudflare deployment configuration
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          R2_BUCKET: ${{ vars.R2_BUCKET }}
          STATIC_DATA_BASE_URL: ${{ vars.STATIC_DATA_BASE_URL }}
          CLOUDFLARE_PAGES_PROJECT: ${{ vars.CLOUDFLARE_PAGES_PROJECT }}
        run: |
          missing=0
          for name in \
            CLOUDFLARE_API_TOKEN \
            CLOUDFLARE_ACCOUNT_ID \
            R2_ACCESS_KEY_ID \
            R2_SECRET_ACCESS_KEY \
            R2_BUCKET \
            STATIC_DATA_BASE_URL \
            CLOUDFLARE_PAGES_PROJECT; do
            if [ -z "${!name}" ]; then
              echo "Missing required Cloudflare deployment setting: ${name}" >&2
              missing=1
            fi
          done
          exit "$missing"

      - name: Install rclone
        run: |
          curl https://rclone.org/install.sh | sudo bash

      - name: Configure rclone for Cloudflare R2
        env:
          R2_ACCESS_KEY_ID: ${{ secrets.R2_ACCESS_KEY_ID }}
          R2_SECRET_ACCESS_KEY: ${{ secrets.R2_SECRET_ACCESS_KEY }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
        run: |
          mkdir -p ~/.config/rclone
          cat > ~/.config/rclone/rclone.conf <<EOF
          [r2]
          type = s3
          provider = Cloudflare
          access_key_id = ${R2_ACCESS_KEY_ID}
          secret_access_key = ${R2_SECRET_ACCESS_KEY}
          endpoint = https://${CLOUDFLARE_ACCOUNT_ID}.r2.cloudflarestorage.com
          acl = private
          EOF

      - name: Upload static data to Cloudflare R2
        env:
          R2_BUCKET: ${{ vars.R2_BUCKET }}
        run: |
          rclone sync frontend/public/static-data/ "r2:${R2_BUCKET}/static-data" \
            --s3-no-check-bucket \
            --checksum \
            --delete-during \
            --transfers 8 \
            --checkers 16

      - name: Strip static-data from Pages bundle
        run: rm -rf frontend/dist/static-data

      - name: Deploy to Cloudflare Pages
        uses: cloudflare/pages-action@v1
        with:
          apiToken: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          accountId: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
          projectName: ${{ vars.CLOUDFLARE_PAGES_PROJECT }}
          directory: frontend/dist
          gitHubToken: ${{ secrets.GITHUB_TOKEN }}
