name: Daily Price Data

on:
  workflow_dispatch:

concurrency:
  group: daily-price-us
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  publish-us:
    if: ${{ github.ref == format('refs/heads/{0}', github.event.repository.default_branch) }}
    runs-on: ubuntu-latest
    timeout-minutes: 90

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install artifact-native price dependencies
        run: pip install pandas yfinance curl_cffi

      - name: Ensure daily price release exists
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          gh release view daily-price-data \
            --repo "${{ github.repository }}" >/dev/null 2>&1 || \
          gh release create daily-price-data \
            --repo "${{ github.repository }}" \
            --title "Daily Price Data" \
            --notes "Automated US OHLCV bundles for artifact-native static-site builds."

      - name: Download US foundation update bundle
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          mkdir -p /tmp/foundation-update
          FOUNDATION_UPDATE_RELEASE="foundation-update-data"
          FOUNDATION_UPDATE_MANIFEST="foundation-update-latest-us.json"
          if ! gh release download "$FOUNDATION_UPDATE_RELEASE" \
            --repo "${{ github.repository }}" \
            --pattern "$FOUNDATION_UPDATE_MANIFEST" \
            --dir /tmp/foundation-update \
            --clobber; then
            echo "No foundation-update-data release yet; falling back to legacy weekly-reference-data." >&2
            FOUNDATION_UPDATE_RELEASE="weekly-reference-data"
            FOUNDATION_UPDATE_MANIFEST="weekly-reference-latest-us.json"
            gh release download "$FOUNDATION_UPDATE_RELEASE" \
              --repo "${{ github.repository }}" \
              --pattern "$FOUNDATION_UPDATE_MANIFEST" \
              --dir /tmp/foundation-update \
              --clobber
          fi
          FOUNDATION_UPDATE_MANIFEST_PATH="/tmp/foundation-update/${FOUNDATION_UPDATE_MANIFEST}"
          export FOUNDATION_UPDATE_MANIFEST_PATH
          echo "FOUNDATION_UPDATE_RELEASE=${FOUNDATION_UPDATE_RELEASE}" >> "$GITHUB_ENV"
          echo "FOUNDATION_UPDATE_MANIFEST=${FOUNDATION_UPDATE_MANIFEST_PATH}" >> "$GITHUB_ENV"

          BUNDLE_ASSET_NAME="$(python - <<'PY'
          import json, os
          from pathlib import Path
          manifest = json.loads(Path(os.environ['FOUNDATION_UPDATE_MANIFEST_PATH']).read_text(encoding='utf-8'))
          coverage = manifest.get('coverage') or {}
          if manifest.get('market') != 'US':
              raise SystemExit(f"foundation update manifest market must be US, got {manifest.get('market')!r}")
          if coverage.get('universe_mode') != 'US_OPTIONABLE':
              raise SystemExit(f"foundation update manifest must use US_OPTIONABLE, got {coverage.get('universe_mode')!r}")
          if int(coverage.get('active_symbols') or 0) < 3000:
              raise SystemExit(f"foundation update manifest active_symbols too low: {coverage.get('active_symbols')}")
          if int(coverage.get('missing_active_symbols') or 0) != 0:
              raise SystemExit(f"foundation update manifest has missing active symbols: {coverage.get('missing_active_symbols')}")
          print(manifest['bundle_asset_name'])
          PY
          )"
          echo "FOUNDATION_UPDATE_BUNDLE=/tmp/foundation-update/${BUNDLE_ASSET_NAME}" >> "$GITHUB_ENV"
          echo "FOUNDATION_UPDATE_SHA256=$(python - <<'PY'
          import json, os
          from pathlib import Path
          print(json.loads(Path(os.environ['FOUNDATION_UPDATE_MANIFEST_PATH']).read_text(encoding='utf-8'))['sha256'])
          PY
          )" >> "$GITHUB_ENV"

          gh release download "$FOUNDATION_UPDATE_RELEASE" \
            --repo "${{ github.repository }}" \
            --pattern "${BUNDLE_ASSET_NAME}" \
            --dir /tmp/foundation-update \
            --clobber

      - name: Verify foundation update checksum
        run: |
          set -euo pipefail
          digest="$(python - <<'PY'
          import hashlib, os
          from pathlib import Path
          print(hashlib.sha256(Path(os.environ['FOUNDATION_UPDATE_BUNDLE']).read_bytes()).hexdigest())
          PY
          )"
          if [ "$digest" != "$FOUNDATION_UPDATE_SHA256" ]; then
            echo "Foundation update checksum mismatch: $digest != $FOUNDATION_UPDATE_SHA256" >&2
            exit 1
          fi

      - name: Download prior daily price artifact if available
        continue-on-error: true
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          mkdir -p /tmp/prior-daily-price
          gh release download daily-price-data \
            --repo "${{ github.repository }}" \
            --pattern daily-price-latest-us.json \
            --dir /tmp/prior-daily-price \
            --clobber

          PRIOR_BUNDLE_ASSET_NAME="$(python - <<'PY'
          import json
          from pathlib import Path
          manifest = json.loads(Path('/tmp/prior-daily-price/daily-price-latest-us.json').read_text(encoding='utf-8'))
          print(manifest['bundle_asset_name'])
          PY
          )"
          gh release download daily-price-data \
            --repo "${{ github.repository }}" \
            --pattern "${PRIOR_BUNDLE_ASSET_NAME}" \
            --dir /tmp/prior-daily-price \
            --clobber
          echo "PRIOR_DAILY_PRICE_BUNDLE=/tmp/prior-daily-price/${PRIOR_BUNDLE_ASSET_NAME}" >> "$GITHUB_ENV"

      - name: Build daily price artifact from foundation update symbols
        run: |
          set -euo pipefail
          args=(
            --foundation-update "$FOUNDATION_UPDATE_BUNDLE"
            --output-dir /tmp/daily-price
            --batch-size 100
            --batch-sleep-seconds 1
            --min-symbol-coverage 0.80
            --fetch-mode all
          )
          if [ -n "${PRIOR_DAILY_PRICE_BUNDLE:-}" ]; then
            args+=(--prior-daily "$PRIOR_DAILY_PRICE_BUNDLE")
          fi
          cd backend
          python -m app.scripts.build_daily_price_artifact "${args[@]}"

      - name: Validate daily price outputs
        run: |
          set -euo pipefail
          test -s /tmp/daily-price/daily-price-latest-us.json
          bundle="$(find /tmp/daily-price -maxdepth 1 -name 'daily-price-us-*.json.gz' | head -n 1)"
          if [ -z "$bundle" ] || [ ! -s "$bundle" ]; then
            echo "Missing daily price bundle" >&2
            exit 1
          fi
          python - <<'PY'
          import json
          from pathlib import Path
          manifest = json.loads(Path('/tmp/daily-price/daily-price-latest-us.json').read_text(encoding='utf-8'))
          if manifest.get('market') != 'US':
              raise SystemExit(f"daily manifest market must be US, got {manifest.get('market')!r}")
          if float(manifest.get('symbol_coverage') or 0) < 0.80:
              raise SystemExit(f"daily manifest coverage too low: {manifest.get('symbol_coverage')}")
          if int(manifest.get('symbol_count') or 0) < 3000:
              raise SystemExit(f"daily manifest symbol_count too low: {manifest.get('symbol_count')}")
          print({
              'as_of_date': manifest.get('as_of_date'),
              'symbol_count': manifest.get('symbol_count'),
              'symbol_coverage': manifest.get('symbol_coverage'),
              'bundle_asset_name': manifest.get('bundle_asset_name'),
          })
          PY

      - name: Upload daily price assets
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          bundle="$(find /tmp/daily-price -maxdepth 1 -name 'daily-price-us-*.json.gz' | head -n 1)"
          gh release upload daily-price-data "$bundle" \
            --repo "${{ github.repository }}" \
            --clobber
          gh release upload daily-price-data /tmp/daily-price/daily-price-latest-us.json \
            --repo "${{ github.repository }}" \
            --clobber
